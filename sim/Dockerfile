# sim/Dockerfile
# Simulator image with STK-compatible propagator
# SentinelHunter Simulation

FROM debian:stable-slim

# TODO: Add STK-compatible propagator and dependencies
CMD ["echo", "Simulator stub running."]
